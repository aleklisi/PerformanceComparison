# Wilcoxon Test

wilcox.test(time ~ test, data = first_and_second_erlang_runs, alternative = "two.sided")

# The p-value of the test is more than the significance level alpha = 0.05.
# We can conclude that first run median weight is not significantly different from the second run median time.

